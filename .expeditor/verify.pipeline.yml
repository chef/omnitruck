steps:
  - label: static
    command: |
      bundle install --binstubs --path=.vendor
      bundle exec rspec
      erb -T- ./kubernetes/deployment.yml| ruby -ryaml -e "YAML.parse(STDIN.read)"
    expeditor:
      executor:
        docker:

  - label: WebServer Tests
    command: |
      .expeditor/buildkite/webserver-tests.sh
    expeditor:
      executor:
        docker:
