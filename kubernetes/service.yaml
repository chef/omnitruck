---
apiVersion: v1
kind: Service
metadata:
  name: omnitruck
  labels:
    app.kubernetes.io/name: omnitruck
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: omnitruck